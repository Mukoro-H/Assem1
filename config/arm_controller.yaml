controller_manager:
  ros__parameters:
    update_rate: 50
    use_sim_time: true

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    arm_controller:
      # Sửa thành JointGroupPositionController để nhận lệnh từ file Python của bạn
      type: position_controllers/JointGroupPositionController

arm_controller:
  ros__parameters:
    joints:
      - khopdich_joint
      - khau1_joint